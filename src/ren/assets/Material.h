#pragma once


#include <string>

namespace ren {
  // Design spec

  // I want to have a material system which is flexible enough to support
  // any artibtrary material (shader + parameter) that I could want in the future,
  // but also simple enough that a simple material can be created without 


  class Material {

    // Fundamentally, a material is just a PSO with some parameters.
    // I need to be able to 

  };
}